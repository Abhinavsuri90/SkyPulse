# Lint policy for `ruff check`. Long f-string report templates are deliberate, so the line length is generous.
line-length = 140
target-version = "py313"

[lint]
select = ["E4", "E7", "E9", "F", "I", "B"]   # pyflakes, core pycodestyle, import order, bugbear
